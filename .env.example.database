# Add these to your existing .env.local file to help prevent database issues

# Database connection limits
DB_MAX_CONNECTIONS=10
DB_CONNECTION_TIMEOUT=10000
DB_IDLE_TIMEOUT=30000

# Retry settings
DB_MAX_RETRIES=3
DB_RETRY_DELAY=1000

# Enable database logging for monitoring
DB_LOGGING=true